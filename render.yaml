services:
  - type: web
    name: vr-ai-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
