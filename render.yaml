services:
  - type: web
    name: vr-ai-server
    env: python
    buildCommand: bash build.sh
    startCommand: bash start.sh
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
